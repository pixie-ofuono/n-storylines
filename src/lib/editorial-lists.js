module.exports = function addEditorialPicks (content, listedArticles) {
	// replace the least viewed relevant articles with the editorial picks
	// but we have stripped out the views at this point
	return content;
}
